{
  "name": "@disciplineos/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Typed HTTP client for the Discipline OS backend — shared across mobile and web surfaces.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./errors": "./src/errors.ts",
    "./schemas": "./src/schemas.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "ky": "^1.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vitest": "^2.1.0"
  }
}
