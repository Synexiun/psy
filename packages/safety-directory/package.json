{
  "name": "@disciplineos/safety-directory",
  "version": "0.0.0",
  "private": true,
  "description": "Crisis hotline directory keyed by (country, locale). Shipped with every surface; static-served on the crisis path. See Docs/Whitepapers/04_Safety_Framework.md §Hotline directory.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./data": "./src/hotlines.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "check:freshness": "node --experimental-strip-types scripts/check-freshness.ts"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vitest": "^2.1.0"
  }
}
