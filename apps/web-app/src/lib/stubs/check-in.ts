/**
 * Check-in stubs — placeholder for Phase 1 expansion.
 *
 * `CheckInStubs` will grow as the check-in flow acquires more UI state that
 * needs deterministic test/Storybook data. Add keys here and in `checkInStubs`
 * together so callers always get a value.
 */

export type CheckInStubs = {
  intensityDefault: number;
};

export const checkInStubs: CheckInStubs = {
  intensityDefault: 5,
};
