'use client';
import * as React from 'react';

export function WordmarkSvg(): React.ReactElement {
  return (
    <span className="font-display text-lg font-semibold tracking-tight text-ink-primary">
      Discipline OS
    </span>
  );
}
