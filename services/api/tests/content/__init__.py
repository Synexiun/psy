"""Content module tests — safety directory, help articles, intervention content.

The safety-directory suite is load-bearing: it enforces CLAUDE.md Rule #10
(freshness gate + mirror parity).  A failing test here blocks a release.
"""
