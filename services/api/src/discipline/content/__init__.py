"""Per-locale content — safety directory, intervention scripts, help articles.

Shipped as data; rendered at read time with locale-appropriate typography and
direction.  The safety directory in particular is the server-side mirror of the
``@disciplineos/safety-directory`` package and must match byte-for-byte at every
release (CI compares the two).
"""
